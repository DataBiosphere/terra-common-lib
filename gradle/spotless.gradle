spotless {
    groovyGradle {
        target '**/*.gradle'
        greclipse()
        indentWithSpaces()
    }
    java {
        googleJavaFormat()
        removeUnusedImports()
        targetExclude "${buildDir}/**"
    }
}
